En este caso es muy similar al codigo para sumar dos numeros. Solo es cambiarle el operador `+` por `%`.

```cpp
#include <bits/stdc++.h>
using namespace std;

int a, b;

int main() {
    cin >> a >> b;
    cout << a % b;
    return 0;
}
```