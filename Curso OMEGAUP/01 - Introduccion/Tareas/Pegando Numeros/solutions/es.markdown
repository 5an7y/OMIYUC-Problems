Para que los numeros aparezcan pegados solo se deben imprimir sin un espacio que las separe.

```cpp
#include <bits/stdc++.h>
using namespace std;

int a, b;

int main() {
    cin >> a >> b;
    cout << a << b;
    return 0;
}
```